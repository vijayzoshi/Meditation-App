package com.kmit.musicapp.interfaces;
/*

import com.kmit.musicapp.Config;
import com.kmit.musicapp.Model.Example;
import com.kmit.musicapp.models.Track;

import java.util.List;

import retrofit2.Call;
import retrofit2.http.Field;
import retrofit2.http.FormUrlEncoded;
import retrofit2.http.POST;
import retrofit2.http.Query;
import retrofit2.http.GET;
import retrofit2.http.Streaming;
*/

/**
 * Created by Harjot on 30-Apr-16.
 */
public interface StreamService {
 /*   @GET("/tracks?client_id=" + Config.CLIENT_ID)
    Call<List<Track>> getTracks(@Query("q") String query, @Query("limit") int limit);

    @FormUrlEncoded
    @Streaming
    @POST("API/home")
    Call<List<Example>> doCreateUserWithField(@Field("home_components_name") String name, @Field("user_id") String jobg, @Field("is_myProfile") String job);
*/
}
